<header class="z-50 fixed top-0 bg-gray-300 w-full h-16">
  <div class="h-full container mx-auto flex items-center justify-between px-5">
    <div class="text-lg font-bold">LOGO</div>

    <% if current_user %>
      <div data-controller="dropdown" class="relative">
        <button data-action="click->dropdown#toggle">
          <%= image_tag current_user.avatar_url, class: 'h-10 w-10 rounded-full object-cover' %>
        </button>

        <ul data-dropdown-target="menu" class="hidden bg-white z-50 text-sm absolute right-0 mt-2 w-32 border rounded shadow p-2">
          <li><%= link_to 'マイページ', '#' %></li>
          <li><%= link_to 'ログアウト', '#' %></li>
        </ul>
      </div>
    <% else %>
      <div class="text-lg font-bold">登録ボタン</div>
    <% end %>
  </div>
</header>
